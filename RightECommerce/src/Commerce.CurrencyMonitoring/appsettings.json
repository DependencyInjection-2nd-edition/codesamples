{
  "ConnectionStrings": {
    // NOTE: While the book's listings use SQL Server, these code samples use SQLite.
    // This allows you to start this application, without having to first create a database.
    "CommerceConnectionString": "Data Source=..\\..\\..\\..\\..\\sqlite.db"
  }
}
